[module/cpu]
type = internal/cpu
interval = 2
format-prefix = "%{A1:st -e htop -s PERCENT_CPU:} %{A}"
format-prefix-foreground = ${colors.primary}
label = %{A1:st -e htop -s PERCENT_CPU:}%percentage:2%%%{A}
