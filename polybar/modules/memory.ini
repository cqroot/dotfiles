[module/memory]
type = internal/memory
interval = 2
format-prefix = "%{A1:st -e htop -s PERCENT_MEM:} %{A}"
format-prefix-foreground = ${colors.primary}
label = %{A1:st -e htop -s PERCENT_MEM:}%percentage_used:2%%%{A}
